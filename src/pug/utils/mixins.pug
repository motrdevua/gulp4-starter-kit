//- SVG Sprite mixin
mixin icon(name,mod)
  if(mod)
    svg(class="icon icon--" + name + ' ' + mod)
      use(xlink:href="assets/img/spriteSvg.svg#" + name)
  else
    svg(class="icon icon--" + name)
      use(xlink:href="assets/img/spriteSvg.svg#" + name)
